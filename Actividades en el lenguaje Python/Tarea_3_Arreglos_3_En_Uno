#Actividad: Tarea 3 Arreglos 3 En Uno
#Alumno: Cristian Pabel Vidaña Herrera
#Fecha: 1/Septiembre/2025
#Materia: Estructura de Datos

#• Realizar un algoritmo que haga el recorrido de un arreglo e imprima sus valores. 

arreglo = [1, 2, 3, 4, 5]
print("Elementos del arreglo:")
for elemento in arreglo:
    print(elemento)


    #• Algoritmo que inserte un valor en un índice (posición) determinado.
posicion = int(input("Ingrese la posición donde desea insertar el valor (0-4): "))
ValorAgregar = int(input("Ingrese el valor que desea agregar: "))
if 0 <= posicion < len(arreglo):
    arreglo[posicion] = ValorAgregar
    print("Arreglo después de la inserción:")
    for elemento in arreglo:
        print(elemento)
else:
    print("Posición inválida. Por favor ingrese una posición entre 0 y 4.")

    #W• ⁠Algoritmo que implemente la búsqueda lineal de un elemento en un arreglo.

elemento_a_buscar = int(input("Ingrese el elemento que desea buscar en el arreglo: "))

for i in range(len(arreglo)):
    if arreglo[i] == elemento_a_buscar:
        print(f"Elemento {elemento_a_buscar} encontrado en la posición {i}.")
        break
else:
    print(f"Elemento {elemento_a_buscar} no encontrado en el arreglo.")

    


